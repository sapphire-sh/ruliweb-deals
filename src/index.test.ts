'use strict';

// import {
// 	App,
// } from '../src';

// const app = new App();

jest.setTimeout(20000);

describe('@ruliweb_deals', () => {
	it('temp', (done) => {
		done();
	});

	// it('start', (done) => {
	// 	app.start().then(() => {
	// 		done();
	// 	});
	// });

	// it('parse', (done) => {
	// 	app.parse(1900, 0).then(() => {
	// 		done();
	// 	});
	// });

	// it('insert', (done) => {
	// 	// app.insert([]).then(() => {
	// 	done();
	// 	// });
	// });
});
